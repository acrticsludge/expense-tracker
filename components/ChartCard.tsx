import { Card, CardContent } from "@/components/ui/card";

export default function ChartCard() {
  return (
    <Card className="h-64 shadow-sm border bg-white">
      <CardContent className="p-6 flex items-center justify-center text-gray-400">
        {/* Placeholder – will replace with Recharts pie chart */}
        <p>Category Breakdown Chart (Coming Soon)</p>
      </CardContent>
    </Card>
  );
}
